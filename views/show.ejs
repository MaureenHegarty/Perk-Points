<!-- http://localhost:8080/coffees/41 (and the id number)-->

<div class="description">
  <h2>Check out this Coffee!</h2>
  <h4>
    Do you know that every time you post or share a coffee from one of our
    affiliated cafes that you are earning points and reaping the rewards of your
    Perk Point membership?<br />
  </h4>
</div>

<center><h2 class="coffee-name"><%= coffee.coffee_name %></h2>

<p></p>
<img src="<%= coffee.image_url %>" alt="" />
<div class="description">
  <h3>Want to delete or update your coffee?</h3>
  <h4>Go right ahead...we love getting our members updates!</h4></center>
</div>

<form action="/coffees/<%= coffee.id %>" method="post">
  <input type="hidden" name="_method" value="delete" />
  <button>delete</button>
</form>

<form action="/coffees/<%= coffee.id %>/edit" method="get">
  <button type="submit">update</button>
</form>

<p></p>
<h3>Comments</h3>

<% if(comments.length > 0) { %> <% for(let comment of comments) { %>
<div class="comment">
  <p><%= comment.content %></p>
  <span class="posted-by">Posted by:</span> <%= user.first_name %> <%=
  user.last_name %>
</div>
<% } %> <% } else { %>
<p>No comments yet.</p>
<% } %> <% if(loggedIn) { %>
<form action="/comments" method="post">
  <input type="hidden" name="coffee_id" value="<%= coffee.id %>" />
  <label for="">Leave your comment here</label>
  <textarea name="content"></textarea>
  <button>post comment</button>
</form>
<% } else { %>
<p>You must be logged in to post a comment.</p>
<% } %>

<div class="bottom-space"></div>
